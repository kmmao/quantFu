# v12-fi.py 集成可视化指南

## 🤔 为什么不能直接运行 v12-fi.py?

**答案**: 因为 v12-fi.py 只包含了"推送模块",还缺少你的"交易策略代码"。

---

## 📦 文件结构对比

### ❌ 你原来的 v12.py (只有策略,没有推送)

```
┌─────────────────────────────────────┐
│ v12.py                              │
├─────────────────────────────────────┤
│ import talib                        │
│ import pandas                       │
│                                     │
│ def market_order():                 │
│     # 开仓逻辑                       │
│     下单()                           │
│     成交()                           │
│     # ❌ 没有推送到 QuantFu          │
│                                     │
│ def close_postion():                │
│     # 平仓逻辑                       │
│     平仓()                           │
│     成交()                           │
│     # ❌ 没有推送到 QuantFu          │
│                                     │
│ def on_bar():                       │
│     # 策略主逻辑                     │
│     ...                             │
└─────────────────────────────────────┘
```

### ✅ 现在的 v12-fi.py (有推送模块,但缺策略代码)

```
┌─────────────────────────────────────┐
│ v12-fi.py                           │
├─────────────────────────────────────┤
│ [第1-365行] QuantFu 推送模块        │
├─────────────────────────────────────┤
│ import requests                     │
│ import os                           │
│                                     │
│ class QuantFuPusher:                │
│     def push_trade():               │
│         # 推送成交数据               │
│                                     │
│ def quantfu_market_order_hook():    │
│     # 开仓推送钩子                   │
│                                     │
│ def quantfu_close_position_hook():  │
│     # 平仓推送钩子                   │
│                                     │
├─────────────────────────────────────┤
│ [第366行开始] 你的策略代码           │
├─────────────────────────────────────┤
│ # TODO: 这里是空的!                 │
│ # 需要粘贴你的 v12.py 代码          │
│                                     │
│                                     │
│                                     │
│                                     │
└─────────────────────────────────────┘
```

### 🎯 集成后的 v12-fi.py (完整版)

```
┌─────────────────────────────────────┐
│ v12-fi.py (完整集成版)              │
├─────────────────────────────────────┤
│ [第1-365行] QuantFu 推送模块        │
├─────────────────────────────────────┤
│ class QuantFuPusher:                │
│     def push_trade(): ...           │
│                                     │
│ def quantfu_market_order_hook(): ..│
│ def quantfu_close_position_hook(): │
│                                     │
├─────────────────────────────────────┤
│ [第366行开始] 你的 v12.py 完整代码  │
├─────────────────────────────────────┤
│ import talib                        │
│ import pandas                       │
│                                     │
│ def market_order():                 │
│     # 原有开仓逻辑                   │
│     下单()                           │
│     成交()                           │
│     ✅ quantfu_market_order_hook()  │  ← 新增这一行!
│                                     │
│ def close_postion():                │
│     # 原有平仓逻辑                   │
│     平仓()                           │
│     成交()                           │
│     ✅ quantfu_close_position_hook()│  ← 新增这一行!
│                                     │
│ def on_bar():                       │
│     # 策略主逻辑                     │
│     ...                             │
└─────────────────────────────────────┘
```

---

## 🛠️ 集成步骤 (只需3步)

### Step 1: 打开 v12-fi.py,定位到第 392 行

```python
# 第390-395行
# ==================== 原 v12.py 代码开始 ====================

# TODO: 将你的 v12.py 完整代码粘贴到这里
# 或者使用 import 方式:
# from v12 import *
```

### Step 2: 将你的 v12.py 全部代码复制进来

**操作方法:**

1. 打开你的 `v12.py`
2. 按 `Ctrl+A` (或 `Cmd+A`) 全选
3. 按 `Ctrl+C` (或 `Cmd+C`) 复制
4. 回到 `v12-fi.py` 的第 392 行 (TODO 位置)
5. 按 `Ctrl+V` (或 `Cmd+V`) 粘贴

**粘贴后应该是这样:**

```python
# ==================== 原 v12.py 代码开始 ====================

import talib
from datetime import datetime
import pandas as pd
# ... 你的所有 import ...

account_id = "85178443"  # 你的账户ID

def market_order(context, content, market_order_type, order_num=0):
    # 你的原有代码
    ...

def close_postion(context, content, market_order_type, order_num, loss_profit_type):
    # 你的原有代码
    ...

def on_bar(context, bar):
    # 你的原有代码
    ...

# ... 你的所有其他函数 ...
```

### Step 3: 在 2 个地方添加推送调用

#### 位置1: `market_order()` 函数 - 成交成功后

**找到这样的代码:**

```python
def market_order(context, content, market_order_type, order_num=0):
    # ... 原有下单逻辑 ...

    # 下单
    order = context.insert_market_order(...)

    # 等待成交
    while not order.is_filled():
        time.sleep(0.1)

    # 成交后
    filled_price = order.avg_fill_price  # 实际成交价格
    order_id = order.order_id            # 订单号

    # ========== 在这里添加推送 ==========
    # (添加下面这段代码)
```

**添加这段代码:**

```python
    # ========== QuantFu 推送 ==========
    try:
        quantfu_market_order_hook(
            account_id=account_id,           # 使用你定义的账户ID变量
            symbol=content['symbol'],        # 合约代码
            direction=market_order_type,     # buy/sell
            volume=order_num,                # 手数
            price=filled_price,              # 成交价格
            order_id=order_id,               # 订单号
            commission=order_num * 5         # 手续费(根据实际情况调整)
        )
    except Exception as e:
        print(f"[QuantFu] 推送失败但不影响交易: {e}")
    # =================================
```

#### 位置2: `close_postion()` 函数 - 成交成功后

**找到这样的代码:**

```python
def close_postion(context, content, market_order_type, order_num, loss_profit_type):
    # ... 原有平仓逻辑 ...

    # 平仓
    order = context.insert_market_order(...)

    # 等待成交
    while not order.is_filled():
        time.sleep(0.1)

    # 成交后
    filled_price = order.avg_fill_price
    order_id = order.order_id

    # ========== 在这里添加推送 ==========
    # (添加下面这段代码)
```

**添加这段代码:**

```python
    # ========== QuantFu 推送 ==========
    try:
        quantfu_close_position_hook(
            account_id=account_id,
            symbol=content['symbol'],
            direction=market_order_type,     # closelong/closeshort
            volume=order_num,
            price=filled_price,
            order_id=order_id,
            commission=order_num * 5
        )
    except Exception as e:
        print(f"[QuantFu] 推送失败但不影响交易: {e}")
    # =================================
```

---

## ✅ 集成完成检查清单

- [ ] **第392行后**: 已粘贴完整的 v12.py 代码
- [ ] **market_order()**: 成交后添加了 `quantfu_market_order_hook()` 调用
- [ ] **close_postion()**: 成交后添加了 `quantfu_close_position_hook()` 调用
- [ ] **account_id**: 已设置为你的极星账户ID (如 "85178443")
- [ ] **手续费**: 已根据实际情况调整 `commission` 计算公式

---

## 🎯 两种集成方式对比

### 方式A: 直接复制代码 (推荐)

**优点**:
- 简单直接,不依赖文件路径
- 便于在极星应用中部署

**步骤**:
1. 复制 v12.py 全部代码
2. 粘贴到 v12-fi.py 的第392行
3. 添加2处推送调用

### 方式B: 使用 import

**优点**:
- 保持文件独立
- 便于版本管理

**步骤**:
1. 确保 v12.py 和 v12-fi.py 在同一目录
2. 在 v12-fi.py 第392行添加: `from v12 import *`
3. **然后仍然需要修改原 v12.py**,添加2处推送调用

**⚠️ 注意**: 即使用 import,你仍然需要修改原 v12.py 添加推送调用,所以推荐直接用方式A。

---

## 🔍 集成前后对比示例

### ❌ 集成前 (v12.py 原代码)

```python
def market_order(context, content, market_order_type, order_num=0):
    """开仓函数"""
    print(f"准备开仓: {content['symbol']} {market_order_type} {order_num}手")

    # 下单
    order = context.insert_market_order(
        symbol=content['symbol'],
        direction=market_order_type,
        volume=order_num
    )

    # 等待成交
    while not order.is_filled():
        time.sleep(0.1)

    # 获取成交信息
    filled_price = order.avg_fill_price
    order_id = order.order_id

    print(f"开仓成功: {order_num}手 @{filled_price}")
    # ❌ 到此为止,没有推送到 QuantFu
```

### ✅ 集成后 (v12-fi.py 中的代码)

```python
def market_order(context, content, market_order_type, order_num=0):
    """开仓函数"""
    print(f"准备开仓: {content['symbol']} {market_order_type} {order_num}手")

    # 下单
    order = context.insert_market_order(
        symbol=content['symbol'],
        direction=market_order_type,
        volume=order_num
    )

    # 等待成交
    while not order.is_filled():
        time.sleep(0.1)

    # 获取成交信息
    filled_price = order.avg_fill_price
    order_id = order.order_id

    # ✅ 新增: 推送到 QuantFu
    try:
        quantfu_market_order_hook(
            account_id=account_id,
            symbol=content['symbol'],
            direction=market_order_type,
            volume=order_num,
            price=filled_price,
            order_id=order_id,
            commission=order_num * 5
        )
        print(f"[QuantFu] 成交数据已推送")
    except Exception as e:
        print(f"[QuantFu] 推送失败: {e}")

    print(f"开仓成功: {order_num}手 @{filled_price}")
```

---

## 🚀 集成后如何使用

### 1. 配置环境变量

在极星应用或系统中设置:

```bash
export QUANTFU_API_URL=http://localhost:8888
export QUANTFU_API_KEY=your-api-key-from-env
export QUANTFU_ENABLE=true
```

### 2. 启动 QuantFu 后端

```bash
cd ~/Documents/GitHub/quantFu
./scripts/start.sh
```

### 3. 运行集成后的策略

在极星应用中:
- 上传 `v12-fi.py` (已包含推送功能的完整版)
- 启动策略
- 每次成交后会自动推送数据

### 4. 查看数据

打开 QuantFu 前端:
```
http://localhost:3000
```

你会看到实时的持仓、成交记录、盈亏分析等。

---

## 🎓 总结

### v12-fi.py 的角色

```
v12-fi.py = 推送模块(已完成) + 你的策略代码(需要你添加)
```

### 为什么不是两个独立文件?

因为推送钩子函数需要在**策略成交后立即调用**,最简单的方式是:
1. 在同一个文件中
2. 在成交成功的代码后直接添加钩子调用

### 核心理念

**非侵入式集成** - 只需在成交后添加2行推送调用,不改变任何交易逻辑。

---

## ❓ 常见问题

### Q1: 我可以不修改 v12.py,单独运行 v12-fi.py 吗?

**A**: 不可以。v12-fi.py 目前只有推送模块,没有交易策略。就像一个"只有发动机没有车身的车",无法运行。

### Q2: 我可以保留 v12.py,让它自动调用 v12-fi.py 吗?

**A**: 技术上可以,但需要:
1. 修改 v12.py 的 import
2. 修改 v12.py 的 market_order() 和 close_postion()
3. 确保文件路径正确

**结论**: 既然都要改 v12.py,不如直接合并到 v12-fi.py 更简单。

### Q3: 如果我的 v12.py 很长 (1000+ 行),怎么办?

**A**: 没问题!直接全部复制粘贴到 v12-fi.py 第392行即可。文件大小不是问题,Python 能处理。

### Q4: 推送失败会影响交易吗?

**A**: 不会。推送代码包在 `try-except` 中,失败只会打印日志,不会中断交易。

### Q5: 我可以禁用推送吗?

**A**: 可以。设置环境变量:
```bash
export QUANTFU_ENABLE=false
```

或在代码中:
```python
quantfu_pusher = QuantFuPusher(enable=False)
```

---

## 📞 需要帮助?

如果你:
- 不确定在哪里添加推送代码
- 不清楚如何获取 `filled_price` 或 `order_id`
- 遇到任何集成问题

**请直接分享你的 v12.py 代码 (或核心部分)**,我可以帮你完成集成。

---

**🎉 集成完成后,你的极星策略就能自动推送数据到 QuantFu 平台了!**
